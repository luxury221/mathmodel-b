# v47：第四问相同整数选点模型的MILP后端对照

## Material Passport

- 2026-09-12，主求解前登记。v45等价约简与v46分层约束均已独立审计，但CP-SAT在固定预算内均未返回候选。
- 假设：同一组0/1覆盖约束的线性松弛分支定界，可能比当前纯布尔搜索更早产生可行网络。本轮不是延长旧模型运行，也不改变模型可行集合。
- 仍是离线几何研究；官方演练0、正式测试0、场景与留出0。所有内容在D盘，不安装或升级依赖。

## 固定试验

- 使用v46批次已经冻结并通过审计的649点、原始覆盖组、v45保留组、17条正整数站数下界；不重新选择区间、不重新拟合LP权重。
- 0/1变量、原点必选、19至20点、50米最小站距、每组至少选一点、各区间的必要站数与最少节点目标均保持不变。
- 仅将布尔模型写成稀疏线性整数模型，调用已安装SciPy 1.18.1的milp/HiGHS。时间上限30秒，mip_rel_gap=0，其余采用现有后端默认设置；不声称两个后端具有相同内部线程实现或比较加速倍数。
- 仅一次主求解，时间截断无候选即记录UNKNOWN；若有尚未证明最优的可行解，记录FEASIBLE_TIME_LIMIT并保留边界与间隙。浮点解必须先接近0/1，再按整数逐行验证全部原始约束，才可视作候选。
- 每条模型行保存类型、成员索引和整数上下界。独立审计重新生成原始公开见证，核对保留关系、站距冲突、层约束及模型行，防止模型翻译漏约束。
- 候选还需原TriangleCoverage严格连续判空，保留所有非空残片及公开几何反例。只有连续完整且代理改善至少1%才允许后续另行冻结的完整任务对照。本轮不对连续失败候选追加求解。
- UNKNOWN不等于无解；求解器的离散不可行报告不等于任意平面位置不存在20点方案。站数下界与建模CPU时间都不是秒/源任务指标。
