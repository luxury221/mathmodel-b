# v46：第四问径向层整数站数约束

## Material Passport

- 2026-09-12，预登记离线几何搜索。v45已将12144组等价压缩为3244组，但固定30秒仍UNKNOWN，没有策略成绩。
- 本轮假设：各径向区间的覆盖要求蕴含整数站数下界，把这些下界显式加入选点模型，可能跳过无效节点分配。
- 官方演练0，正式测试0，任务场景0，新留出0，所有文件和缓存在D盘。不是演练成绩或连续平面最优性证明。

## 固定机制

1. 仍用v44的649点、原点固定、非原点9层各72点、原始公开见证和几何余量，站距至少50米，总站数19至20。v45的等价约简和全部原始覆盖约束保留。
2. 对9层的全部45个非空连续区间，以区间内节点为变量，仅取所有可选点都在该区间内的原覆盖组，求分数集合覆盖的线性规划下界。每个LP用现有SciPy/HiGHS，最多3秒；失败只记失败，不用未验证数值生成约束，不重试。
3. LP仅建议非负对偶权重。权重乘10^8向下取整；分母增至不小于任一接收点累计整数权重。用纯整数重新检查每点预算，所以不依赖求解器浮点容差作证明。
4. 每个覆盖组需命中一点，故区间选点数至少为对偶有理目标的向上取整。每个新增约束保存非零权重、对应原组索引、分母和全部整数核验信息。原点不计入这些区间，但全局下界要加必选原点1。
5. 利用互不重叠的径向区间可将下界相加。用前缀最长路求最强区间组合下界，保存其不交区间链；此诊断只适用于此有限点池与见证，不推广到任意接收位置。
6. 将全部正下界作为冗余有效约束加入v45布尔选点模型。CP-SAT仍单线程、种子0、最多30秒，最少节点目标不变。本轮只运行一次强化选点模型；不追加未变化模型的时间。

## 资格与审计

- 先用三角覆盖的3/2分数下界、精确超预算归一化、空区间、互不相交/相交区间下界组合、非法证明和小规模全部赋值验证，正确性通过后冻结源码与协议。
- 独立审计器不调用生产证书/组合下界计算函数，直接按原组逐点累加整数权重，核验每条新增约束确实必要；核验已知21点网满足全部新增约束。
- 返回候选必须满足全部原覆盖组、最小站距，并通过原TriangleCoverage严格连续判空；非空区域完整保存，不删残片，不把有限见证覆盖当连续覆盖。
- 仅当候选连续完整且费用代理改善至少1%才进入另行冻结的完整任务适配与对照；若首次候选连续失败则保留反例供新协议研究，不在此模型里重复求解。
- UNKNOWN不是无解。只有已核验整数证书证明下界超过20，或本有限模型返回INFEASIBLE，才可说明此有限点池中19/20点方案被排除；不得声称连续平面中不存在20点方案。
- 总目标与Q4 v11基线保持不变，不把约束数减少、搜索分支数或站数下界报告成秒/源改善。
