# v45：第四问覆盖约束等价化简

## Material Passport

- 日期：2026-09-12。类型：离线几何可行性实验，不是完整任务成绩。
- 状态：先登记，后实现、正确性测试、冻结，再运行主求解。
- 假设：v44的覆盖组含严格冗余；删除被较小组蕴含的约束，可能使相同预算内的选点搜索更有效。
- 官方演练调用0，正式测试0，新场景0，新留出0，所有生成内容在D盘。

## 保持不变

直接使用冻结v44的649点池、13392位置/方向见证、接收半径999.9米和方向投影0.01米。原点必选，选19至20点，任意两站距离至少50米，以点数最少为目标。求解器仍为单线程CP-SAT、种子0、每轮30秒，最多8轮。有未知或不可行状态但没有候选时立即停止，不重复相同模型或追加时间。不重跑v44的原始未知模型；仅与其完整历史输出比较，不宣称跨批次运行速度加速倍数。

## 允许的唯一变化

1. 覆盖组含必选原点，则恒成立，删除并记录原点见证。
2. 若覆盖组A包含于B，则约束“至少选A中一点”蕴含“至少选B中一点”，可删除B。每个删除项必须给出最终保留子组的原始索引，不能仅引用另一个已删除组。
3. 重复组按上述规则删除。空组必须保留并导致不可行，不能静默忽略。
4. 使用整数位集加速；独立审计用集合包含关系验证。禁止列支配删点、改变几何余量、放松站距、删覆盖残片或旋转到有利场景。

## 执行与门槛

- 小规模枚举覆盖族和所有布尔赋值，验证固定原点前提下化简前后可行集合完全相同；另测试重复、空组、固定点、索引验证及确定性。
- 正确性测试通过后冻结全部源码、协议与所依赖历史源码；主运行前写入文件SHA256和源码副本。
- 每轮保存全部原始覆盖组、保留组、蕴含见证、求解元数据。有限见证候选仅是搜索候选，必须送入原TriangleCoverage连续判空，并完整保存非空残片。
- 连续失败候选仍按v44方法加入最多64个公开几何反例和集合排除约束。UNKNOWN不等于INFEASIBLE；离散池不可行不等于连续平面无解。
- 保留原21点网作已知可行对照，核对全部原始/化简组、站距和连续证书；点数条件不对21点对照作合格候选判定。
- 候选须连续全覆盖、费用代理较原网下降至少1%，方可进入另行冻结的完整任务试验。没有合格候选时，不新增策略或编造秒/源结果。
- 若进入任务试验，必须修正新坐标适配流程：不能仅传stations而保留会重建旧网的network='dual21'入口；必须审计实际站序列、remaining_stations和CertifiedPatrol缓存。本协议不修改冻结客户端。

## 结论界限

约束压缩率不是任务提速。只有新网通过完整任务、独立物理回放和后续演练，才可能替换Q4 v11基线。失败和未知结果完整保留；不挑场景或重新使用留出集调参。
