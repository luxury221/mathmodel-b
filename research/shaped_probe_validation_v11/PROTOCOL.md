# v11候选独立验证协议

日期：2026-09-11。在扩展开发实验完成之前登记本验证程序，不改变v11参数。

1. 合并v11六类10/13/16源共18个开发场景。四种模式必须全部完成，失败不剔除。
2. 从narrow70、narrow85、shaped_cost中选案例等权均值最低者；差距在1秒/源以内时优先简单固定模式narrow85、再narrow70。
3. 只有开发均值优于冻结基线至少1%才启动新留出。一次只验证一个候选，不能在留出上重选或调参。
4. 保存选型、源码哈希、源码副本后生成新留出24例（6类×10/12/14/16源）与压力8例。
   拟用基数231100000、237100000；与整个reports目录内已有场景种子做去重审计，有交集则停止，不自动改种子。
5. 压力包含源域/5米/20米/1000米边界、弱接收朝向、固定极端误差和空间相关误差。
6. 同案例基线certified_fixed与新候选配对，全清、真值包含、清除证书、真实光学证据及逐动作时钟均须通过。
7. 报告留出、压力、合并的全部均值及逐例退步。不将离线结果当成官方成绩。
8. 进入下一轮离线候选的门槛：全部正确、合并均值改善至少1%、留出和压力均值都不退步、最坏单例退步不超过15%。
9. 做两次确定性重放。通过绩效门槛后才做本机回环HTTP正常/故障恢复检查，不接触官方模拟器。
10. Q4<300与Q3<180全局目标未达时保持active；本次只验证第四问，不因本阶段完成而结束目标。

所有写入、缓存和HTTP日志留D盘。原客户端、v4、v5、v7与验证依赖不修改。
