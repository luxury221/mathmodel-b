# Q3保留延迟定位的覆盖重分配 v23

首次绩效运行前登记；Q3<180、Q4<300完整任务案例等权秒/源目标不变，真实模拟器调用必须为0。
冻结station_only及59份依赖不改。重用v21的几何覆盖规划器，不重用其全部源优先调度。
原有130米区域半径门槛、pilot观测决策、局部定位、机会测向、清除证书、实际覆盖与最大16源停止条件保持。

模式previous=station_only；identity=新的调度适配但不移动扫描点、不增扫描，必须与原策略逐动作哈希一致；
repack=允许完整证书通过后的未来扫描点重分配；repack_scan=再允许v21原有完整费用门槛通过的当前位置额外扫描。
规划费用改为同一门槛下当前目标中心与未来扫描点的联合开放路线，加未知频道扫描费，与实际调度保持同一候选集合。
仍只是计划费用，不是全任务最优证明。未来点不算真实观测；非空覆盖残片不能忽略。

先对三类不同新单测场景验证identity逐动作复现，再跑既有103100000开发基数的六类13源四模式。
若identity任一主运行轨迹不同则该批不用于性能结论，先修复适配。
其他候选全清且至少省1秒/源才扩展10/16源；近似平局优先简单repack。
扩展后至少改善1%才独立验证，冻结后生成不与全部历史reports/单测重叠的新种子。
全部新文件和缓存仅D盘，不修改正式客户端。
