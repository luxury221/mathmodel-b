# Q4保留角度与次序的最小双环收缩 v28

v27未进入绩效：6个单独缩内环方案全部留下连续非空覆盖残片，999米内环也留下约3.131平方米，不能略去。
原先假设存在有效网络的4项单测在共享fixture处报错，而非算法全任务失败；保留这一失败，不宣称v27单测或绩效通过。

新的公开几何修复仅允许内外环一起做小幅径向收缩，所有角度与原站点索引严格保持。
内半径[999,997.5,995]米、外半径[1869,1868,1867,1866,1865.5]米；每个点均由原坐标一次缩放得到。
完整无线覆盖证明不扣除光学盘，所有非空残片一律拒绝。
15个候选中按(1000-内半径)^2+(1870-外半径)^2选择离原网络最近的有效者；无有效者即不跑绩效。
不是以开发案例结果选择半径，也不在同一案例里使用不同细微坐标对同频道反复刷新误差。

仍比较previous、network_only、sentinel、radio_first四模式，分别隔离网络变化与原点光学策略。
身份适配必须与原候选逐动作相同；原点全背向且哨兵未命中的反例仍必须全清；full_route不能跳过实际非空覆盖。
6类13源先开发，至少省1秒/源才扩展10/16源；1秒内近似平局优先network_only、sentinel、radio_first；
18例全清且至少改善1%才冻结、去重新种子并独立验证。任何失败不剔除，不用旧留出调参。
所有源表仅评价器访问，策略只接收公开网络和真实API观测。正式客户端、59份冻结文件不改，官方调用0，文件及缓存只在D盘。
