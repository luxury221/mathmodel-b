# v5/v7双候选独立验证预注册

候选冻结选择：Q3 radial_patrol_v7/pilot1600；Q4 joint_search_v5/certified_fixed。
与冻结v4的Q3 adaptive、Q4 sweep_previous在同一新场景配对。
目标仍为Q3<180、Q4<300案例等权总虚拟秒/源；禁止真实测试，官方调用数必须为0。

留出种子基数113100000，六类x10/12/14/16源，每题24例；压力种子基数117100000，每题8例。
压力使用接收半径1000、几何边界距离、弱可见朝向；分别覆盖固定极端误差和空间平滑相关误差。
所有种子必须与过去公开规则离线生成的数据不重合；代码哈希及源代码副本在运行前归档。

不得排除失败后计算均值，不得依验证结果调整候选。全部场景检查全清、真值包含、清除覆盖、合法区域和独立逐动作计时。
四个场景做确定性重放；四种场景执行本地HTTP（其中两种另加五类故障恢复），校验幂等和每一步6位小数权威计时。
只允许MockArena回环地址，不访问官方服务或内部数据。

即使比v4改善，只要未验证180/300双目标，目标保持active，不能标完成；若候选泛化退化，保留旧可靠版本。

测试执行方式：旧研究目录存在多个顶层experiment.py，合并进同一个pytest解释器时发生模块缓存名称冲突。
第一次合并收集错误保留；按各研究测试文件在新Python进程内隔离执行，不修改冻结旧研究模块。
