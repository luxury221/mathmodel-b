# Q3真实清除点的覆盖增益驱动扫描 v22

首次绩效运行前登记。目标与离线限制不变。v21把扫描与下一批巡查点绑定进行单步费用筛选，
在六类13源仅执行2次额外扫描；源优先本身更慢，未形成沿清除点继续发现的机制。

本轮只改变是否在真实清除位置执行未知频道扫描的准入条件，其余沿用v21 source_repack。
当前点真实未覆盖面积增益/6倍未知频道数，与全部现有计划扫描点的“面积增益/(移动秒+扫描费)”最大值比较。
前者更高才立即扫描。它不需要当前这一点单独抵消某个旧站的完整费用，允许多次真实观测形成互补覆盖。
位置距已有真实扫描至少25米。扫描后重新分配真实未覆盖区域，余下扫描计划仍必须连续完全覆盖。
增益只是调度代理，不是真实发现次数，不假设未知源服从任何已知场景分布，不提前宣布完成。

模式previous=station_only；control=v21 source_repack；frontier=新准入条件。
先6类13源三模式配对；全清且较previous至少省1秒/源才扩展10/16源，不能只比较差control。
扩展后至少改善1%才新留出，先冻结后去重，不读旧留出调参。真实调用0，所有文件D盘。
