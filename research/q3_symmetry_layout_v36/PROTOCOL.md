# v36：第三问首次真实观测后的旋转布局

目标仍以官方演练Q3均值<180秒/源、Q4<300且全清为准。本阶段只做Q3离线开发，不改正式客户端。
已有反复缩放半径、追踪门槛和局部排路负结果不重跑。本轮只改变原点真实扫描之后的整网旋转自由度。
首次原点测向完成后，以当时真实可行域中心/NEAR位置作为目标代理，在0/5/10/.../55度的12个六重对称相位中选择联合路径最短者。
没有发现目标则不旋转；代理相同则保留原网。旋转前后站点数、半径、检测及清除物理费用相同。
每个候选网仍用真实原点覆盖记录的副本加未来站点做完整连续判空，未通过不能采用；未来站点绝不写入实际覆盖。
只旋转尚未访问的初始外部六点，原点不动。其后的pilot1600判断、真实负信息、目标准入和停止证书全部保持原策略。

## 固定比较

- previous：ScanEconomyPolicy(station_only)。
- identity：新适配器不旋转，完整轨迹必须与previous相同。
- joint_phase：只在首次原点扫描后选择一次旋转相位。

旧开发基数103100000，六类13源共18次。平均至少减少1秒/源且最坏退步不超过10%、6/6全清才能扩大10/16源。
若不满足则拒绝，不进一步调相位步长或代理权重，不启用新留出或官方演练。
单测用361423001和工厂基数361423100，只验证正确性，运行前审计与历史种子无碰撞。
所有生成文件、缓存与临时数据均在D盘，冻结依赖不改。策略不能获知真实源数、位置、类型或profile。
静态代理更短不是任务更快的证据；完整任务指标包括所有末尾动作，不筛掉慢例或失败。
