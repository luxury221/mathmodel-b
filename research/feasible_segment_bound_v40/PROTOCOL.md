# v40：两条已知安全偏移方向的条件收益界

只诊断v39两次真实绑定事件，不重新运行策略，不调参，不读官方隐藏信息或新留出。所有文件在D盘，官方调用0。

沿原固定站到同侧原V端点的线段，使用12次二分找已通过严格连续覆盖证书的可行位置。非空残片始终不视为空。
从已失败点的覆盖剩余中构造物理不可见源，接收半径1000米，朝向用接收点的最大角缺口确定。源位置必须在公开1800米域内，且避开已实际执行的光学覆盖。

保留其他扫描点时，该源在可变接收点上的可见区间是圆盘条件与半平面条件的交集。原站能接收、失败点不能接收，因此从二次方程与线性方程计算线段可行比例的必要上界。

用v39已保留的正确性种子401739251核验构造源：保留站均no_signal、已有光学点未清除、原站有信号、失败点无信号。它只作局部正确性反例，不是10—16源规模的性能样例。

若站点最多移动delta米，在访问顺序和其他动作均不变且此点只出现一次时，相邻两条移动边的总缩短不超过2*delta米，即2*delta/5秒。这个界不限制新信息触发的后续策略变化，也不是完整任务或所有算法的收益上界。

只有找到了真实物理见证才报告必要上界；找不到时只报告充分可行位置，不把保守残片当物理漏检证明。
