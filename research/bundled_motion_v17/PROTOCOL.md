# 第四问：保留探测开销的成组访问协议 v17

在本版本任何绩效运行前写入。v16首轮终点排序退步，原因假设为只放入清除中心却省略必需探测入口。
本轮新机制不是继续调整v16阈值：每个未解决源建立“下一次真实动作入口、预计清除位置出口”访问组，
巡查站的入口和出口相同。按前组出口到后组入口的非对称距离优化整条组路线，实际只执行首组的下一步，然后重算。
预计出口不算实际观测、清除或覆盖，不得用于删减真实可行域。全部证书与收费不变。

预设模式：previous=v11 shaped_cost；bundled=成组调度；bundled_transit=成组调度+v16原封不动的途中测向。
成组路线不多于9组时精确DP，否则最多8个近入口起点的最近邻+保持组内方向的2-opt，保留全部组。
比较仍为既有开发基数103100000、6类13源，全部结果保留；全清且均值至少省1秒才考虑扩展10/16源。
不得使用任何旧留出调参；候选独立验证前必须冻结源码并生成不重叠的新种子。
本轮所有动作只在OfflineRuleWorld；真实调用0，所有新文件及缓存仅D盘。
