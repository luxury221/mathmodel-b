# v38事后诊断：为何唯一低成本双点组合被拒绝

这是主实验完成后开展的探索性机制诊断，不是新候选调参或独立验证。
只复现旧开发集max_radius、13源的paired轨迹，在连续证书调用外围记录被拒绝组合；不改变选择、动作或代价。
必须与已冻结主实验的完整轨迹哈希一致。

对剩余区域代表点，按公开1000米最小接收半径及定向半平面规则寻找确实不可见的朝向。
如果找到，用已保留的正确性种子361425001及单个构造源核验全部无线扫描和已观测光学位置均未发现该源。
构造源只是局部物理反例，不是符合10—16源规模的绩效案例，也不是新留出；不将诊断运行为提速证据。
如只存在保守几何残片而未找到物理反例，必须如实记录，不能宣称实际漏检。
所有文件、缓存位于D盘；官方调用0，正式0；不修改冻结主实验及原策略文件。
