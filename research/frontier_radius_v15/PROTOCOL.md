# v15：Q3接近连续覆盖边界的巡查半径

2026-09-11。保持Q3<180、Q4<300案例等权总虚拟秒/源、全清与零真实测试目标，所有产物在D盘。
冻结此前v4/v5/v7/v11；v13已完成18开发场景，源码不在本轮修改。禁止利用旧留出集调参。

v13削减定位中UNKNOWN扫描后，开发均值约231秒/源，仍不足以达到180。
v14经验点试探相对同基线仅约1秒/源收益，不将此微弱先验收益作为主要下一步。

本轮只检验更外侧的巡查位置：仍由原pilot观测规则决定是否向外扩展，
只把扩展半径从1600改为1700或1725米，并对1725加入station_only扫描消融。
6个等间距接收圆与原点圆在内环边界的几何关系提示接近sqrt(3)*1000米的上限；
这只是选取候选的推导线索，不能替代实际起始站保留时的完整连续覆盖检验。
每次扩展仍调用原完整覆盖证书，不完整则失败，不容忍面积残片，也不采用其它未见观测。

模式为previous（pilot1600）、station_only（v13）、pilot1700、pilot1725、pilot1725_station_only。
不同时引入路径优化或经验位置先验；策略不读取真实源数、场景类型或源位置。
先用公共几何和单测确认覆盖，再运行既有六类13源×5模式30次；有收益再补10/16源。
开发选型后冻结唯一候选并使用新的独立留出，不能因为某个开发快例而宣称达到目标。
