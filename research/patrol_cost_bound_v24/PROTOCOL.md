# Q4固定巡查费用诊断 v24

这是诊断，不是新策略，也不是所有算法的不可达证明。真实模拟器调用为0，所有输出、临时文件留D盘。
冻结的59份依赖、正式客户端及历史实验快照不改。先登记此文件，再执行诊断。

## 要回答的问题

在仍必须访问公开21点网络的限定条件下，单独改定位或排路，是否有充分空间达到300秒/源？
只读取既有103100000开发批次的previous记录、动作日志和公开网络，不读取场景源表。
只分析10/13源记录；16源能因最大源数提前停，不能套用强制全网条件。

## 松弛与边界

1. 21个固定扫描点是半径0的必访邻域。每个历史成功清除点对应半径40米的乐观清除邻域：
   同一源的新旧成功清除点都离源不超过20米，故两清除点之间不超过40米。
2. 邻域间边费用为max(0,中心距离-两半径)。这只是下界，不要求各边实现同一邻域内的同一个点。
3. 原点固定起点、终点自由。加入与原点连边固定为1的零费用虚拟节点，解无向二因子MILP并迭代子环割。
4. 每轮只累积求解器的mip_dual_bound，绝不把可行路径的fun当下界。超时照样保留有效松弛界；
   标注HiGHS浮点求解器数值边界，并减去1e-4米数值余量。连通最优松弛不等于原邻域路线最优。
5. travel_plus_clear下界适用于明确强制访问21点、全清的类别；再加每个不存在频道在每个点至少5秒
   的full_scan下界，只适用于每个不存在频道确实必须测全部21点的更窄类别。完全忽略换频、真实源测向和失败光学费用。
6. 实际候选允许替换/删站、光学发现和16源提前停止，不能不加条件把这两个下界套到实际候选，
   也不能当成官方成绩、全局不可能性证明或goal阻塞依据。

## 检查和产物

小规模穷举验证起点/自由终点、邻域距离、松弛下界方向和子环处理；12个开发记录不用于新留出。
保存每轮MILP的状态、dual/primal、子环数、耗时、输入记录与轨迹哈希和源码快照。所有数值从真实输出汇总。
