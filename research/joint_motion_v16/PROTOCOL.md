# 第四问：完整任务联合移动，开发协议 v16

状态：开发阶段；本协议在本版本第一次绩效运行前写入。

目标是完整任务总虚拟秒/源的案例等权均值，不是单源局部定位时间。
仅 OfflineRuleWorld；可在选型后使用本机回环 MockArena。禁止真实模拟器、网络接口、隐藏源信息。
策略只能得到动作响应，不知道实际源数、场景名称、源位置与类别。
保留连续发现覆盖、完整光学清除证书、实际计时、最大16源停止条件。
冻结 v11/v13 及其59份依赖不改动，所有输出在D盘且新建目录。

## 假设与预设消融

- previous：冻结v11 shaped_cost。
- terminal：同一回合的源访问顺序按当前可行域中心排序，而不是临时探测点；执行仍是经过证书的真实下一步。
- transit：保持原调度，在即将走的直线路段上选择可显著改善已知源定位的真实观测停点。
- combined：同时使用上述两个机制。

途中候选为当前路段的25%、50%、75%、100%点及已知源中心在线段上的投影；
候选必须距当前位置至少60米、距该频道已有测点至少50米；每频道最多3次途中测向。
只评估尚不能用最多4个光学圆清除的已知源。
代理测向残差只用于打分，不更新真实可行域或覆盖证据；所有观测真实收费。
按已有正负观测产生的代理样本估计接收概率、残差改善，估计净收益至少10秒才插入。
一次实际停留最多测6个已知频道，执行后重新规划，避免前往已经过时的探测点。

开发：沿用既有开发基数103100000，先6类×13源×4模式；全部失败保留。
开发均值改善不足1秒/源视为近似平局，选择更简单模式；不得使用旧留出调参。
有希望才扩至6类×10/16源；选择必须基于同案例对照且全清。
未选中方法保留负结果，不宣称达到300秒目标。
独立验证前冻结新源码，生成新种子并对全部reports场景及单测种子去重。
