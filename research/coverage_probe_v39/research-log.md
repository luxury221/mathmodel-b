# v39研究日志

- 从v38真实漏检反例出发，改为约束探测点生成，而不是组合既有追踪位置。
- 先证明非对称两点的全可行域范围收缩、射线夹持和镜像恢复；用二阶锥约束生成点，并用独立连续覆盖证书最终验收。
- 登记station_bind与coverage_nudge两个候选、恒等控制、六类13源、费用与扩展门槛；全部原源码不动。
- 新正确性种子通过历史去重审计。13项测试通过，构造场景验证了第一点no_signal时仍保留镜像，且真实扫描完成前不删巡查站。
- 24次完整任务全部全清，7042动作计时及物理回放一致。两个实际绑定承诺额外验证历史无线/光学证据、整个凸包的范围/方向证明及扫描时间区间。
- station_bind两次实际替换均令完整任务变慢；coverage_nudge未通过任何连续覆盖检查。按原门槛拒绝两者，不扩展，不使用新留出或官方案例。
- 28项原策略/连续覆盖/双点守卫回归通过。
- 做14次事后几何检查，得到2个小幅离开固定站仍安全的点，较大位移出现覆盖残片；没有据此调整参数或重新运行性能选择。
- 保留可复用几何证明，不保留性能候选。后续需区分“有连续可行点”和“完整任务能提速”，不能再只据开放路线代理作结论。
