# Q4保留原巡查布局的选择性光学 v27

v26六例24次全清，但换网消融450.456660→466.917379秒/源；sentinel在新网内省3.192308秒/源，
仍比原候选慢，因此v26按原协议拒绝，不扩展、不新留出。
本轮不是挑选表现好的角度，而是消除这个已观察到的换网混杂：保留原网每个外点的实际坐标、原索引顺序和调度。

只从公开几何选择原内环1000米向内收缩的最小幅度，使“完全不依赖原点光学”的连续覆盖证书成立。
依次尝试内半径999、997.5、995、990、985、975米，外点、原点及原布局所有角度完全不变；取第一个完整证书。
这是一次改变网络几何，不在同一案例内使用细微坐标差重复刷测向噪声。已经实际测过的点不移动。
原点和外环坐标保持冻结实现的浮点表示，不重新生成或旋转它们。

模式previous=原shaped_cost；network_only=仅安全缩内环；sentinel=沿用v26的频道1/2实际光学哨兵；
radio_first=沿用v26的实际near触发原点光学。策略不能知道实际源数、源真值、场景类型。
未来规划不算观测、不能忽略非空覆盖；新策略的full_route兜底也必须核实当前真实连续覆盖为空。

先用identity（不缩内环、不省光学）在新单测场景逐动作复现原候选，并验证背向近源/光学遗漏不会成为停止证明。
再跑103100000开发基数六类13源×四模式24次，不从已用留出调整方案。
至少省1秒/源才扩展10/16源；1秒以内近似平局优先network_only、sentinel、radio_first；
18例全清且至少改善1%才冻结代码并新种子去重后独立验证。失败不剔除、每次行动都真实计费。
真实模拟器调用0，正式客户端不改、59份冻结依赖不改，所有生成内容留D盘。
