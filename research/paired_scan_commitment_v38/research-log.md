# v38研究日志

- 核对原v11真实动作调度、v5连续覆盖与单点替换，以及v10曾经未触发的贪心联合覆盖；登记新的显式双点执行承诺，不修改冻结代码。
- 新增双点选择、强制前缀费用、互补见证拒绝筛选、连续覆盖验证、真实扫描后删站及失败保留兜底。
- 种子审计在测试前发现初拟工厂与v28相撞，改用381738127；修复审计把本轮已拒绝预留误判为历史使用的自碰撞。三个审计文件保留，成功记录为unit_seed_audit_v3.json。
- 初版10项测试通过，补充规划器必须触发及拒绝假见证两项测试后12/12通过；主实验前冻结协议和65份源码。
- 固定旧开发六类13源完成18次任务，全部全清；新策略与原基线逐动作一致，平均提速0。没有触发扩展门槛，不使用新留出或官方案例。
- 对5283动作逐项核时并进行18次完整物理回放；原59份依赖及v33的63份批次文件一致。
- 回归命令首次误用了不存在的test_shaped.py文件名，没有运行测试；纠正为test_shaped_probe.py后23/23通过，保留两份XML日志。
- 事后诊断复现max_radius一例，282动作与冻结主实验完全相同。捕获唯一费用合格但连续覆盖失败的组合，验证其6875.378392平方米残片里确有不可见的定向源。
- 另审计真正执行双扫描后删站的构造正确性样例：117动作、2站删除、计时误差0；这不是性能样例，不与主实验的零替换混算。
- 拒绝本版本，不以放宽连续覆盖或微调搜索上限制造收益；下一步研究合法探测点与外缘覆盖缺口的联合生成。
